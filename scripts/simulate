#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cargo build -p cascade-cli --bins --manifest-path "$ROOT/mint/Cargo.toml" >/dev/null
exec "$ROOT/mint/target/debug/cascade-sim" "$@"
